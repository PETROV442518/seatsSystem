<div class="office-layout">
    <!-- Embed the SVG directly -->
    <svg width="800" height="600">
        <image x="0" y="0" width="100%" height="100%" xlink:href="data:image/svg+xml;base64,[Base64 SVG Data]"></image>
    </svg>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        // Select all seat elements in the SVG
        const seats = document.querySelectorAll('.seat');

        seats.forEach(seat => {
            seat.addEventListener('click', function () {
                // Handle seat click event, e.g., show seat details or make a reservation
                const seatId = seat.getAttribute('data-seat-id');
                // You can use AJAX to interact with your Razor Pages backend here.
                // For example, open a reservation form in a modal dialog.
            });
        });
    });
</script>

